name: a_simulator
description: Simulator of bank.

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

  google_fonts: ^5.1.0
  flutter_inappwebview: ^5.7.2+3
  flutter_svg: ^2.0.7
  lottie: ^2.6.0
  youtube_player_flutter: 
  rx_shared_preferences: ^3.0.0
  charts_flutter:
  shared_preferences:
  smooth_page_indicator: ^1.1.0
  # onesignal_flutter:
  url_launcher:
  in_app_review: 
  dio:
  flutter_simple_calculator:
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

